/*
 * stm32f1xx_hal_conf.h — empty shim.
 *
 * Satisfies any transitive #include "stm32f1xx_hal_conf.h" that may appear in
 * headers pulled in during the host-native build. The real HAL conf is not
 * needed because stm32f1xx_hal.h (this shim set) provides everything directly.
 */
#ifndef STM32F1XX_HAL_CONF_H
#define STM32F1XX_HAL_CONF_H
/* intentionally empty */
#endif /* STM32F1XX_HAL_CONF_H */
